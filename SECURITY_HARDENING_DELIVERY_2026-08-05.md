# Amine Web 安全加固交付报告

日期：2026-08-05  
范围：一期高优先级安全加固与低频生产验证

## 1. 执行边界

本次停止了公网自动攻击扫描与并发压测。安全验证改为本地单元/集成测试、静态检查和生产低频冒烟，避免触发云平台或网络安全告警。

生产备份位于：

```text
/root/backups/anime-security-20260805T023403Z
```

备份包含 PostgreSQL、上传文件、私信文件、环境变量、Nginx、systemd 配置和部署 Git 版本，并已通过 SHA-256 校验。

## 2. 基线发现

- 服务器持续收到 PHPUnit、ThinkPHP、目录穿越和 Docker API 等互联网自动扫描请求，当前均返回 404。
- 生产没有 Redis，SlowAPI 限制仅保存在单进程内存中。
- 帖子列表接受 `limit=1000000`，虽然实际数据量较小，仍属于资源消耗入口。
- 任意 Host Header 可以到达应用。
- 登录在第 21 次请求开始返回 429，但缺少 `Retry-After`，且重启后计数清空。
- 恶意 Origin 和 `null` Origin 的 CORS 预检被拒绝。
- Swagger/OpenAPI 在生产关闭。
- 基础 SQL 注入探测没有改变搜索结果，也没有触发数据库错误。
- 匿名目录穿越被拒绝；私信下载要求登录。
- 跨用户读取草稿返回 404；跨用户修改、删除评论返回 403。
- 资料更新测试暴露“数据库已提交，但响应模型校验失败返回 500”的风险，说明输入必须在提交前严格验证。

## 3. 已完成的代码加固

### Redis 与限流

- 新增覆盖全部 `/api/v1` 路由的 Redis 限流中间件，未单独声明的接口仍有默认限制。
- 登录、邮件验证码、搜索、发帖、评论、资料、上传、私信和管理员激活使用独立策略。
- 登录用户同时按用户 ID 和 IP 计数；匿名访问按 IP 计数。
- 429 响应包含 `Retry-After`。
- 生产环境缺少 Redis 时拒绝启动；Redis 运行故障时写操作返回 503，公开读取进入受限降级模式。
- 原 SlowAPI 装饰器也切换到 Redis 存储。

### 输入和数据库边界

- 帖子标题、正文、摘要、分类、标签增加长度与数量限制。
- 搜索关键词、结果数量、分页偏移增加上限。
- 用户资料字段增加长度和控制字符检查，额外管理字段直接返回 422。
- 头像、封面只允许本站上传目录或配置的七牛域名。
- 用户关系计数改为数据库 `COUNT`，避免加载全部关系记录。
- 管理员密钥改用常量时间比较并限制输入长度。

### 文件和私信附件

- 上传改为 64KB 分块读取并设置 10MB 硬上限。
- 根据文件真实签名识别 JPG、PNG、GIF、MP3、WAV、MP4，扩展名和内容不一致时拒绝。
- 图片执行解码、尺寸/帧数检查和重新编码，去除尾部复合载荷与元数据。
- 文件名只由服务端 UUID 生成。
- 私信附件增加数据库所有权记录，并可绑定唯一接收者和消息。
- 未绑定附件仅上传者可读；绑定后仅收发双方可读；无记录文件返回 404。
- 私信文件 key 只接受固定 UUID 文件名，不再接受任意路径形式。

### WebSocket 与浏览器

- WebSocket 校验 Origin。
- 每用户最多 3 个连接，单帧最大 8KB。
- 每连接最多 10 个事件/10秒、60个事件/分钟。
- WebSocket 私信正文与 REST 一致，最多 2000 字。
- 前端和 API 增加 CSP、HSTS、`nosniff`、frame deny、Referrer-Policy 和 Permissions-Policy。
- Vercel 生产构建默认不生成源码映射。

## 4. 服务器配置

- Redis 6 已安装，只监听 `127.0.0.1:6379`。
- Redis 使用独立认证用户、AOF、128MB 内存上限和 TTL 淘汰策略。
- Redis 密钥仅保存在服务器权限为 600 的配置与 `.env` 中。
- Nginx 增加每 IP 30 请求/秒、突发 60、并发连接 20 的外层保护。
- 普通请求体限制为 1MB，仅上传接口允许 11MB。
- `/static/uploads/` 只接受 UUID 文件名和允许的媒体扩展名。
- Nginx 关闭版本显示，并增加安全响应头。
- systemd 已启用只读系统目录、禁止提权、私有临时目录、能力清空、768MB 内存上限、128 任务上限和文件句柄上限。
- Redis 已设置 `vm.overcommit_memory=1`，并写入持久化 sysctl 配置。

## 5. 验证结果

- 后端安全边界测试：5 个测试函数通过。
- Python 语法编译：通过。
- 修改文件 Ruff 静态检查：通过。
- 前端 ESLint：通过。
- Vite 生产构建：通过。
- Git diff whitespace 检查：通过。
- 测试覆盖：参数超限、批量字段越权、媒体 URL 白名单、私信 key 穿越、伪文件内容和图片尾部载荷清理。
- 生产正常访问检查发现历史帖子存在空正文；严格的输入约束曾导致列表响应校验返回 500。现已拆分输入与输出模型：新内容仍拒绝空正文，旧内容可以正常读取。
- 生产首页已用真实浏览器普通访问验证；Vercel 安全响应头生效，API 帖子列表恢复正常后无业务请求错误。

当前上线版本：

```text
第一批安全加固：1e34c66
历史帖子兼容修复：以本报告所在最新 main 提交为准
```

## 6. 留给后续处理的安全事项

以下项目没有在一期交付中强行上线，以避免认证和部署回归：

1. 将网页 JWT 从 `localStorage` 迁移到 HttpOnly Cookie、刷新令牌轮换和 CSRF Token。
2. 将 bcrypt 平滑迁移到 Argon2id，并增加服务端会话撤销。
3. WebSocket 使用一次性 ticket，彻底移除 URL 中的长期 JWT。
4. 管理员激活增加当前密码校验，或改为仅服务器命令授予。
5. 将后端目录迁出 `/root` 并使用专用非 root 系统用户运行。
6. 处理 React Router 高危告警和编辑器依赖的 nanoid 中危告警；当前 React Router 告警涉及未使用的 RSC 模式。
7. 增加验证码错误次数、账号级渐进延迟、验证码/密码重置后的会话撤销。
8. 使用 CDN/WAF 抵御真正的分布式 DDoS；当前 Nginx/Redis 主要处理单源和应用层滥用。
9. 为 Redis、PostgreSQL、Nginx 和应用日志接入持续告警平台。

## 7. 回滚

- 代码：回退到部署前 Git 提交并重启 `anime.service`。
- 数据库：附件迁移支持 Alembic downgrade；必要时恢复 `database.dump`。
- Nginx：恢复 `anime.conf.codex-security-backup` 并执行 `nginx -t` 后重载。
- Redis：移除 `.env` 中的 `REDIS_URL` 前必须先回退到不强制 Redis 的旧代码。
- 完整数据与配置可以从本报告第 1 节的备份目录恢复。
