# Amine Web 账户模块 P0 修复交付

日期：2026-08-07
范围：现网账户功能紧急恢复，不包含 Cookie/刷新令牌认证重构

## 1. 登录成功路径返回 500

- 现象：正确账号和密码登录时，前端显示 `Failed to fetch`。
- 根因：SlowAPI 启用 `headers_enabled=True` 后，会在业务函数返回成功结果时尝试向 Starlette `Response` 注入限流头；现有登录函数返回普通字典且没有注入 `Response` 参数，因此在业务完成后抛出 500。
- 修复：关闭 SlowAPI 自动响应头注入。SlowAPI 的 Redis 计数、装饰器限流和拒绝逻辑仍然启用；统一的安全限流中间件继续负责 429 和 `Retry-After`。
- 验证：增加回归测试，锁定 `_headers_enabled is False`，并通过真实临时账号执行一次生产登录。

## 2. 验证码、注册和密码重置可能“实际成功但页面失败”

- 现象：验证码可能已经发出、账户可能已经创建、密码可能已经修改，但接口最终仍返回 500。
- 根因：这些接口使用了与登录相同的 SlowAPI 装饰器，成功返回阶段会触发同一个响应头注入异常。
- 修复：与登录共用同一项限流配置修复，消除成功返回阶段的异常；不改变业务数据和接口结构。
- 验证：运行后端回归测试，并检查生产日志中这些路由不再出现 `parameter response must be an instance`。

## 3. 普通上传和私信附件可能“文件已写入但页面失败”

- 现象：文件可能已经保存，浏览器仍收到失败提示，用户重试后可能产生重复文件。
- 根因：上传和下载端点也使用相同 SlowAPI 响应头注入模式。
- 修复：关闭自动注入但保留限流执行，避免写盘成功后在响应阶段变成 500。
- 验证：既有文件内容校验测试继续通过，并检查上传端点无同类异常。

## 4. 未处理的后端 500 被浏览器显示为 `Failed to fetch`

- 现象：前端无法读取真实 HTTP 状态和错误信息，只能显示网络请求失败。
- 根因：最外层中间件产生的异常响应没有经过 CORS 中间件，缺少允许来源头。
- 修复：最外层安全中间件捕获未处理异常，返回固定 JSON；对于精确匹配白名单的 Origin 补充 CORS 头，并返回请求 ID。完整堆栈只写服务器日志，不发送给客户端。
- 返回示例：`{"detail":"The server could not process the request","request_id":"..."}`。
- 验证：自动测试断言 JSON、请求 ID、CORS 与安全响应头存在，同时断言内部异常内容不会泄漏。

## 5. 安全策略保持情况

- Redis 共享限流：保留。
- SlowAPI 路由限流：保留。
- 全接口 429 与 `Retry-After`：保留。
- HSTS、CSP、frame deny、`nosniff`：保留，统一异常响应同样携带安全头。
- CORS：只回显配置白名单中的精确 Origin，不接受任意来源。

## 6. 本次未混入的后续问题及具体修复方案

以下问题不属于 P0 热修提交，避免认证架构调整扩大现网恢复风险；它们将在后续兼容版本中处理。

| 问题 | 后续修复方案 |
|---|---|
| JWT 位于 localStorage | 网页端迁移到 HttpOnly、Secure、SameSite=Lax Cookie；提供一次性旧令牌迁移接口，成功后删除本地令牌。 |
| 访问令牌只有 30 分钟且无刷新 | 使用 15 分钟访问 Cookie和7天轮换刷新 Cookie；并发 401 只触发一次刷新。 |
| 退出、重置和封禁不能撤销旧令牌 | JWT 增加会话 ID；Redis 保存会话状态；退出撤销当前设备，重置和封禁撤销全部设备。 |
| Cookie 写请求可能遭受 CSRF | 使用可读 CSRF Cookie + `X-CSRF-Token` 双提交，并精确校验 Origin。 |
| bcrypt 静默截断 72 字节 | 新密码改用 Argon2id；旧 bcrypt 在成功登录后自动升级；新密码限制 8–128 字符。 |
| 邮箱大小写导致登录失败 | 邮箱标识登录前统一去空白并转小写；用户名保持现有大小写规则。 |
| WebSocket URL 暴露长期 JWT | 改为 Redis 保存的60秒单次 ticket，连接和重连前重新申请。 |
| 资料保存前端假成功 | PATCH 成功后才更新本地状态与跳转；失败时保留编辑页并显示后端错误。 |
| 学校、班级、简介等字段无法清空 | PATCH 明确把空字符串解释为清除，不再转换成 `undefined`。 |
| 旧头像和头图缓存复活 | 后端用户资料作为唯一来源；清除字段时同步移除旧 localStorage 媒体缓存。 |
| 注册和重置成功提示消失 | 切换到登录模式时保留一次性成功提示，只有新的用户操作才清除。 |
| 临时网络错误强制退出 | 仅认证 401 进入刷新/退出流程；403、5xx 和网络错误保留会话并展示可重试状态。 |
| 验证码发送和消费不原子 | SMTP 接受邮件后才写入 Redis；错误5次失效；数据库提交成功后才消费验证码。 |
| 缺少账户集成测试 | 覆盖登录、Cookie属性、刷新轮换、退出、撤销、迁移、验证码、注册、重置、资料清空和头像链路。 |

## 7. 回滚

若发布后发现异常，恢复发布前提交并重启 `anime` 服务即可。本次不包含数据库迁移，也不改变现有 JWT 和前端协议，因此无需数据回滚。
