# 后端 API 基础地址
# 开发环境：使用相对路径，经 Vite proxy 转发，避免跨域问题
# vite.config.js 中已配置 /api → http://localhost:8000
VITE_API_BASE_URL=/api/v1

# 生产环境请修改为你的实际域名
# VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
